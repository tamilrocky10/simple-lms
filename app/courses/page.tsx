import {createClient} from '@supabase/supabase-js';import Link from 'next/link';
export default async function Courses(){const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const {data}=await s.from('courses').select('*,lessons(count)').order('created_at',{ascending:false});return <main className="max-w-6xl mx-auto p-6"><Link href="/" className="text-blue-600">← Home</Link><h1 className="text-4xl font-bold mt-6">Courses</h1><div className="grid md:grid-cols-3 gap-5 mt-8">{data?.map((c:any)=><Link key={c.id} href={'/courses/'+c.id} className="card"><div className="h-32 rounded-xl bg-gradient-to-br from-indigo-500 to-blue-500"/><h2 className="font-bold text-xl mt-4">{c.title}</h2><p className="text-slate-500 mt-2">{c.description}</p><p className="text-sm mt-4">{c.lessons?.[0]?.count||0} lessons</p></Link>)}</div></main>}